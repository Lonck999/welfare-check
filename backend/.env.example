# Neon (Serverless PostgreSQL) 連線字串，從 Neon 專案 dashboard 取得
DATABASE_URL=postgres://user:password@host/dbname?sslmode=require

# 後端監聽埠號
PORT=3000

# Sentry 錯誤追蹤（後端專案，跟前端是不同的 Sentry 專案/DSN）
# 只有 NODE_ENV=production 時才會初始化，本機開發留空或不設 NODE_ENV 皆不會回報
SENTRY_DSN=
NODE_ENV=
