# 專案維護規範

本文件是給 Claude Code 在維護 `welfare-check` 這個 Skill 專案時遵守的規則，非使用者問卷內容。

## 核心檔案

- `skills/welfare-check/SKILL.md`：本體，問卷題目（第一步）＋搜索清單（第三步）都在這裡
- `README.md`：對外功能摘要，含「共 N 大類」字樣
- `網站化規劃.md`：未來網站化規劃文件，也有引用「N 大類」字樣與搜索清單邏輯
- `.claude/progress.md`：跨對話工作進度留檔

## 修改 SKILL.md 時必須遵守的規則

### 1. 新增/修改搜索項目時，回頭檢查問卷是否需要跟進

每個搜索項目（第三步）背後都依賴問卷（第一步）收集到的資料做判斷（例如：居住縣市、工作地點、是否有自有住宅、家庭成員健康狀況等）。

**新增或修改一個搜索項目的判斷條件時，必須確認：**

- 判斷條件所需的資料，問卷裡**是否已經有對應題目**在收集？
  - 有 → 不用加題目，直接在搜索清單註明用的是第幾題資料（沿用本次「社會住宅依工作地點加搜」的做法）
  - 沒有 → 必須在第一步新增對應題目（或子題），並在受影響的搜索項目下方加註明是靠哪一題判斷
- 反過來，**新增問卷題目時也要檢查**：這一題蒐集到的資料，是否已經在某個搜索項目被使用？如果目的是為了支援某個新邏輯，要在該搜索項目寫清楚依據第幾題。

不要讓「問卷有問但沒人用」或「搜索邏輯需要資料但問卷沒問」的情況發生。

### 2. 編號穩定，不可任意重排

SKILL.md 裡多處用「（41）」「（47）」這種編號引用其他搜索項目（例如第 14 題附註影響第 41、42、43...項）。

- **新增項目一律往後加編號**（目前到 58，下一個從 59 開始），不要插隊重排前面的編號
- 若真的需要移除某個項目，該編號**留空或標註「已停用」**，不要讓後面的編號往前遞補，以免跟其他段落的引用對不上

### 3. 分組（搜尋執行順序）要同步維護

第三步有「搜尋執行順序」11 組分類。新增搜索項目時：

- 判斷它屬於哪一組（資格認定/時效性/現金補助/醫療/住宅/就業/特殊身分/教育/稅務貸款/地方政府/一般優惠），把編號加進對應組別
- 如果新項目時效性高（有明確截止期限、名額有限、急迫性），優先考慮放進「時效性最高項目」組

### 4. 修改搜索項目數量時，同步更新引用類別總數的文件

只要 58 這個數字有變動（新增/刪除搜索項目），必須同步檢查並更新：

- `README.md`：「涵蓋 **58 大類**」字樣
- `網站化規劃.md`：目前有 2 處引用類別數（截至本次為止已修正為 58，注意未來若再變動要一併找出所有出現處，不要只改一處）

用 Grep 搜尋數字（例如舊數字 `40` 或現在的 `58`）確認有沒有遺漏的地方，不要只憑記憶改。

### 5. 每次維護結束前更新 progress.md

依照 `留檔範本.md` 的格式，把本次改了什麼、還有什麼沒做完、有沒有收集到但還沒寫入的使用者個資，寫進 `.claude/progress.md`。

### 6. 個資保護紅線

執行 `/welfare-check` 問卷時收集到的使用者實際個資（出生年月日、收入、財產、健康狀況、家庭成員資料等）：

- **絕對不可**寫入任何會被 git 追蹤的檔案（包含 `.claude/progress.md`、commit message、CLAUDE.md 本身等）
- 唯一合法的落地位置是 `welfare-result-YYYYMMDD.html`，且該檔案已加入 `.gitignore`
- 若需要在 progress.md 留檔「對話中收集到但尚未寫入的資料」，只能記錄**題號與資料類型**（例如「已問到第 9 題收入，尚未問第 11 題健康狀況」），不可記錄實際數值

### 7. 雲端排程（門檻更新／新補助發現）分支保護

`welfare-check-threshold-update` 排程（trigger id：`trig_01JSQPCpLYLsShUoTGekN7KL`）**每週日凌晨 1 點**（台北時間；cron `0 17 * * 6` UTC）比對 SKILL.md 裡的金額/門檻是否符合最新 gov.tw 公告，**同時也要檢查是否有現行搜索清單沒涵蓋到的新補助/福利項目**。原本是每月 1 號執行一次，因部分補助時效性極短（申請截止日、候補梯次、名額有限），拉長到一個月才檢查一次容易讓使用者錯失權益，故改為每週執行：

- **發現金額/門檻異動**：直接更新 SKILL.md 對應數字
- **發現全新的補助/福利項目**（現有 58 大類都沒涵蓋到）：
  1. 依規則 2 用下一個新編號加入搜索清單，不可插隊重排
  2. **查看該補助是否有時限**（申請截止日、梯次、名額有限、限期公告等），若有時效性，依規則 3 歸進「搜尋執行順序」的「時效性最高項目」組，並在項目說明裡註明期限性質
  3. 依規則 1 檢查問卷（第一步）是否已有對應題目可判斷這項新補助的資格條件：
     - 若既有問卷資料已足夠 → 直接在新項目下方註明依據第幾題判斷
     - 若問卷沒問過 → 必須新增對應題目（或子題），否則這個新項目永遠無法被正確篩選
  4. 依規則 4 同步更新 README.md／網站化規劃.md 的類別總數
- **同一次排程也會檢查 `backend/src/db/seed-data/*.ts`**（網站資料庫的建檔來源檔）：逐筆重新查證每個 `SeedBenefit` 的 `sourceUrl` 是否仍支持其 `description`/`notes` 裡的金額或門檻，有異動就直接改 seed-data 檔案並更新該筆的 `lastVerifiedDate`；來源網址查證失敗（404/改版/疑似下架）則保留該筆並加註 ⚠️，不自動刪除；**這個檢查只改 TypeScript 原始檔，不會執行 `npm run seed`、不會碰 Neon 正式資料庫**——要讓異動真正生效，必須人工 review 該分支後自己跑 `cd backend && npm run seed`
- 上述異動（無論是 SKILL.md 門檻數字、新項目、還是 seed-data 資料庫內容）都遵守同一套流程：「有異動 → 開新分支 `update-thresholds-YYYYMMDD` ＋ 在 `更新紀錄/` 留檔（須寫明新增項目的編號、時限、是否新增問卷題目，以及 seed-data 異動/疑似下架清單）＋ push 新分支，不直接動 main」
- 若是人工手動更新（非排程觸發），**也要走同樣流程**：開分支、留檔、再讓使用者 review 後決定是否合併到 main，不要直接改 main 上的 SKILL.md
- 檢查排程是否跑過，用 `git fetch` + `git branch -r` 看有沒有 `update-thresholds-*` 分支，不要用本機 `CronList`（那只列本次 session 建立的排程，看不到 claude.ai routine）

### 8. 搜尋語句中的佔位符不可寫死

SKILL.md 搜索清單裡的 `{年份}`、`{縣市}`、`{鄉鎮市區}`、`{工作地點縣市}`、`{居住縣市}`、`{離島縣市}` 都是佔位符，代表執行問卷當下才會代入的動態值。

- 新增/修改搜索語句時，維持佔位符原樣，**不要**手滑寫成當年度實際年份（如 `2026`）或範例縣市（如 `台北市`）
- 若要新增範例，用 Markdown 引言或括號註明「例如」，不要直接取代佔位符

### 9. 條件守門詞（若…）不可在整理時被誤刪

搜索清單裡「若租屋」「若無自有住宅」「若家中有 65 歲以上成員」這類條件句，決定該項目**要不要**被搜尋。

- 重新排序、分組、複製貼上整理格式時，必須逐項核對條件句是否完整保留，不可只搬編號跟標題、漏掉條件
- 修改後建議用 Grep 搜尋 `若` 這個字，跟修改前的行數/項目數量比對，確認沒有條件句在整理過程中消失

### 10. 資料庫化時，「即時性內容不存」不等於「整個項目都不存」

網站化（`backend/src/db/seed-data/`）把福利資料轉成結構化資料時，遇到「內容會頻繁變動、存了很快過期」的項目（例如社會住宅的空戶/候補梯次、招租公告），正確做法是：

- **不存**會過期的即時內容本身（不要把「現在有哪些空戶」寫死存進資料庫）
- **但要存**穩定不變的部分：主管機關、申請入口網站/電話、基本資格門檻、常態性说明
- 每筆資料要誠實標註「即時可用性需至入口網站查詢，資料庫不保證即時」

不要因為「這項資料會過期」就把整個福利項目排除在資料庫外——那樣使用者在網站上會完全查不到這項福利存在，比資料稍微不夠即時更嚴重。判斷標準永遠是「使用者至少要能查到去哪裡查」，不是「資料庫要完全即時」。

### 為什麼會漏掉子項目：根本原因與具體對策

第一次全部 56 大類建檔完成後，使用者兩次追問「還有沒有跟社會住宅一樣的漏洞」，兩次都真的抓到新漏洞（第一次是第 2 類的孕產婦補助/流產假，第二次是第 5/18/22/54/56 類共 6 個子項目）。事後檢討，漏掉的原因不是不知道要做，而是**執行方式本身就會系統性漏掉子項目**：

1. **用「這類別的代表性關鍵字」搜尋，而非逐條搜尋 SKILL.md 列出的每一行 `搜尋：` 語句。** 例如第 2 類 SKILL.md 明明列了 6 條獨立搜尋語句（生育給付／孕產婦補助坐月子／生育獎勵金／陪產假／育嬰留停／流產假+流產諮商），但因為「生育獎勵金」最容易查到大量整理文章，實際只搜了 1～2 次就覺得「查得差不多了」，其餘 5 條語句沒有真的逐一下去搜。
2. **批次趕進度時，用「查到的資料量」而非「SKILL.md 條列的子項目數」判斷一個類別是否完成。** 一次處理 5 個類別、每類別只花 1～2 次搜尋，很容易把「這個類別已經有東西了」誤判為「這個類別做完了」。
3. **沒有機械化的收尾檢查。** 只有在使用者主動追問時才回頭做逐條比對，代表這個檢查步驟原本就不是建檔流程的必經步驟，而是事後補救。

**具體對策（往後任何一次新增/重建福利類別建檔，都必須執行）：**

- 建檔前，**先把該類別在 SKILL.md 裡的每一條 `搜尋：` 語句抄出來，列成一份子項目清單**（可以是暫時的 scratch 筆記），清單項目數 = 該類別最少要有的獨立資料主題數（不是資料列數，因為一個子項目縣市不同會展開成多筆）。
- 每個子項目都要**至少各自跑一次 WebSearch**，不能因為前面的子項目查到很多資料，就假設後面的子項目「應該也差不多」或「應該有查到」。
- 完成後，**逐項核對清單**：每個子項目在 seed 資料裡至少有一筆對應資料（哪怕是「⚠️ 未查得，需另行查證」的誠實佔位），核對不完不算這個類別建檔完成。
- 不要用「這個類別已經有 N 筆資料」當作完成的判斷依據——資料筆數只反映縣市展開的多寡，不反映子項目覆蓋率，兩者是不同的維度，必須分開檢查。
