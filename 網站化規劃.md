# welfare-check 網站化規劃

> 建立日期：2026-06-20
> 目標：把目前的 Claude Code Skill（welfare-check）改造成獨立網站，供他人查詢使用

---

## 一、背景與現況

目前 `welfare-check` 是一套 Claude Code Skill（見 [skills/welfare-check/SKILL.md](skills/welfare-check/SKILL.md)），運作方式：

- 靠 Claude Code 環境本身的多輪推理＋WebSearch/WebFetch 工具，逐題問答收集使用者資料
- 即時搜尋政府網站，比對 58 大類補助資格
- 輸出 HTML 報告

**這套運作方式無法直接搬到網站上**，因為它依賴 Claude Code 這個互動環境的 agent 能力，不是一支獨立程式。

---

## 二、核心困難點（排除金流）

### 1. 核心邏輯要整套重寫
[SKILL.md](skills/welfare-check/SKILL.md) 是寫給 Claude Code agent 看的「指示文件」，不是可獨立執行的程式。要做成網站，需要重建後端：自己管理對話狀態、自己呼叫 LLM API、自己處理工具呼叫迴圈與錯誤重試。

### 2. 即時網路搜尋的依賴最難複製
現在的核心價值在於即時查詢最新補助資訊。網站環境沒有 Claude Code 內建的搜尋工具，自接搜尋 API（Google/Bing）品質不穩定，容易抓到過期或非官方頁面。

### 3. 資料時效性的長期維運成本
補助項目（尤其地方政府加碼）會隨政策、年度預算不斷變動，需要長期人力或自動化機制持續追蹤更新，否則網站給出的資訊很快過期。

### 4. 回應時間與使用體驗
單次查詢若靠即時搜尋＋多次 LLM 推理，可能要數十秒到一兩分鐘，需設計成非同步流程（背景處理＋通知），不能讓使用者卡在頁面上等。

### 5. 併發與成本控管
多人同時查詢會疊加 API 呼叫量與費用，需要排隊機制、rate limiting，避免流量上升時成本失控或被惡意灌爆。

### 6. 個資蒐集與法律免責
使用者要填年齡、所得、財產、健康狀況等敏感資料，蒐集/儲存/傳輸都受個資法規範；資格判斷如有誤導致使用者錯失補助，需要明確免責聲明設計。

---

## 三、解決方案：資料庫化 + 規則比對引擎

**核心決定**：事先把所有福利、單位、所需文件等資訊存進資料庫，使用者填完資料後直接查資料庫比對資格，不在查詢當下呼叫 LLM 或即時搜尋。

### 優點
- 不再依賴 Claude Code 環境，邊際成本大幅下降（接近零），回應速度快
- 對「單次收費查詢」的商業模式更合理（成本可控）

### 帶來的新困難
1. **資格邏輯比想像中複雜**：排富條款、家庭人口計算方式、動產/不動產認定、特殊例外情況等，需要把政策文字轉換成結構化判斷規則（if-else／decision tree），且每次政策修改都要回頭改程式邏輯
2. **資料維護變成長期、持續的人力成本**（見第四節）
3. **涵蓋範圍可能反而變窄**：只能涵蓋事先建檔的項目，新政策或地方小眾加碼如果沒收錄會直接漏掉
4. **正確性責任更重**：資料庫資料過期或規則寫錯導致誤判，責任更明確地落在維護者身上

---

## 四、長期維運：每月批次更新流程

**策略**：用 Claude Code（之後可進一步自動化為 Claude API 腳本）每月跑一次 WebSearch/WebFetch，重新核對資料庫內容，而不是每次使用者查詢都即時搜尋。

### 1. 資料庫欄位要先加上「來源依據」
每筆福利資料除內容本身，需多存：
- `source_url`（官方來源網址）
- `source_excerpt`（從該網址擷取出來、支撐數值/門檻的原文片段）
- `last_verified_date`（最後確認日期）

### 2. 比對流程：只輸出「有來源佐證」的異動
每月批次針對資料庫內每一項福利：
- 重新搜尋＋WebFetch 該福利的官方頁面
- 從頁面擷取目前的金額/門檻/所需文件
- 跟資料庫現有值比對，**只有新舊不同時才列入異動清單**
- 異動清單格式：`福利名稱 | 欄位 | 舊值 | 新值 | 來源網址 | 原文引用片段`

### 3. 強制要求「網址必須真實可打開且內容對得上」
- AI 必須先用 WebFetch 實際抓到該網址內容，把比對所需的原文片段直接複製貼到異動清單裡（原文引用，不是 AI 自己改寫的描述）
- 如果 WebFetch 抓不到網址（404、改版、找不到對應內容），這筆異動標記「無法驗證」，**不能列入正式異動清單**，需人工另外處理（可能該福利已取消或改了網址）

### 4. 人工審核才能寫入資料庫
- 異動清單輸出成表格（Markdown / CSV），人工逐行核對：舊值 → 新值，點來源網址確認
- 只有「核准」的異動才寫入資料庫；未核准的不動，下個月再檢查一次
- **AI 的角色是「找差異 + 附證據」，寫入資料庫的決定權永遠在人工**

### 5. 新福利／疑似下架的福利要分開處理
- AI 搜尋到資料庫沒有的新福利 → 另列「新增候選清單」，一樣要附來源網址
- 資料庫裡的福利，AI 搜尋不到對應頁面 → 標記「疑似已下架」，**不自動刪除**，等人工確認

### 6. 版本紀錄
每次更新保留 diff/log，記錄誰核准了什麼異動、何時生效，方便回溯與除錯。

### 7. 對使用者的透明標示
網站需明確標示「資料更新日期：YYYY/MM/DD」，並在免責聲明中說明「請以各單位最新公告為準」，劃清時效落差的責任界線。

---

## 五、待辦：自動化批次更新（暫緩，排在後面做）

目前構想是先用 Claude Code 手動跑每月更新，長期則需要把這個流程寫成腳本，用 Claude API（+ web search tool）排程自動執行，不依賴人工每月記得操作。

**架構待規劃項目（之後再展開）：**
- Claude API 呼叫腳本（含 web search tool 串接）
- 排程機制（cron / 雲端排程服務）
- 異動清單產出格式與審核介面
- 審核通過後寫入資料庫的流程

---

## 六、暫不考慮：金流整合

商業模式設想是單次查詢收費 10 元，但金流整合（綠界/藍新/TapPay 等多要求公司登記、且需評估 API 成本是否高於 10 元）**先不在本次規劃範圍內**，待核心查詢功能與資料維護機制穩定後再評估。

---

## 七、整體步驟總覽

**前提確認**：全端自己寫程式開發；使用者不需註冊帳號，每次查詢都是一次性填表即查即丟（不保留歷史記錄，最簡單）。

### 階段 1：技術選型與基礎建設

**1.1 技術棧選型（已決定，見對應 Issue）**
- [x] 前端框架：**Vue 3 + Vite**（先不用 Nuxt，之後視需要再整合，取得 SSR/檔案路由等全端功能）— [#3](https://github.com/Lonck999/welfare-check/issues/3)
- [x] 後端框架/語言：**Node.js + Fastify**（與前端同語言、學習曲線低，效能優於 Express）— [#4](https://github.com/Lonck999/welfare-check/issues/4)
- [x] 資料庫：**Neon（Serverless PostgreSQL，代管服務）**（不需要 Supabase 內建 Auth，因確定不做帳號系統）— [#5](https://github.com/Lonck999/welfare-check/issues/5)
- [x] ORM：**Drizzle**（TypeScript 原生、輕量，與 Neon 搭配為社群常見組合）— [#6](https://github.com/Lonck999/welfare-check/issues/6)

**1.2 網域與主機（已決定，見對應 Issue）**
- [x] 網域：**先用 Railway 免費子網域**（如 `xxx.up.railway.app`），待有實際需求再購買正式網域 — [#7](https://github.com/Lonck999/welfare-check/issues/7)
- [x] 主機/雲端代管平台：**Railway**（前後端都部署在此，對 Node.js 支援度好）— [#8](https://github.com/Lonck999/welfare-check/issues/8)
  - **已實際部署上線並驗證**：專案 `reasonable-comfort` 底下兩個 service：
    - `welfare-check`（backend，Root Directory `backend`）：https://welfare-check-production.up.railway.app
    - `welfare-check-frontend`（frontend，Root Directory `frontend`）：https://welfare-check-frontend-production.up.railway.app
  - 已用 Playwright 對正式站網址跑完整填表→送出→結果頁流程，確認前端成功呼叫正式站後端 API、Neon 資料庫查詢正常、無 console error
  - 過程中修正兩個部署相關問題：(1) `frontend/package.json` 原本沒有 `start` 指令，Railway 不知道怎麼把 `vite build` 產出的靜態檔案serve 起來，新增 `vite preview` 作為 start script，port/host 設定改放 `vite.config.ts`（用 `process.env.PORT` 讀取，避免 shell 語法在 Windows/Linux 不相容）；(2) Vite preview server 預設會擋掉不認得的 Host header（DNS-rebinding 防護），Railway proxy 轉發的網域被擋，加上 `preview.allowedHosts: true` 解決
  - **已知問題**：Railway 這個 repo 的 GitHub App webhook 目前卡住，push 到 `main` 不會自動觸發部署（`Deployment queued due to upstream GitHub issues`，但查證 GitHub／Railway 官方狀態頁當下都顯示正常，判斷是這個專案的 GitHub 整合本身卡住，嘗試過分支/repo 重新連接皆未解決）。**目前繞過方式**：改用 Railway CLI 的 `railway up`（`npm install -g @railway/cli` → `railway login --browserless` → `railway link` → `railway up --ci -s <service>`）從本機直接上傳部署，不經過 webhook。之後每次要上線新版都需要這樣手動跑一次，或等 webhook 復原
- [x] SSL：**使用 Railway 免費子網域自動配發的 SSL**，免額外設定；之後綁定正式網域時 Railway 也支援自動申請憑證 — [#9](https://github.com/Lonck999/welfare-check/issues/9)
- [x] 流量/併發上限：**先用免費方案測試**，上線後依第 1.4 節監控工具觀察實際用量，再評估是否升級付費方案 — [#10](https://github.com/Lonck999/welfare-check/issues/10)

**1.3 環境與部署流程**
- [x] 建立 Git repository，規劃分支策略（如 main / dev）— 已建立 `dev` 分支，後續開發在此進行，穩定後再合併回 `main`
- [x] 建立本地開發環境，可連線測試用資料庫
  - [x] `frontend/`：Vue 3 + Vite + TypeScript 專案骨架（已 `npm install`，可 `npm run dev` 啟動）
  - [x] `backend/`：Fastify 專案骨架（TypeScript，`npm run dev` 用 tsx 啟動，已驗證 `/health` 端點回應正常）、Drizzle 設定檔 `drizzle.config.ts` 與 schema（見階段 2）
  - [x] 已連線 Neon 正式資料庫（連線字串存在 `backend/.env`，已 gitignore）：階段 4 開發 `/api/check` 時已用 curl 與瀏覽器多次實測，確認可正確查詢/寫入並回傳計算結果
- [x] 建立 staging（測試站）與 production（正式站）環境分離：Railway 專案 `reasonable-comfort` 用 `railway environment new staging --duplicate production` 建立 staging environment，`welfare-check`／`welfare-check-frontend` 兩個 service 在 staging 下改綁 `dev` 分支（`railway service source connect --branch dev`），production 維持綁 `main`；兩邊共用同一個 Neon 資料庫（使用者選擇：網站不落地使用者資料，staging 查正式庫無汙染風險，不需為此多開一個資料庫）；staging frontend 的 `VITE_API_BASE_URL` 另外設定指向 staging backend（不是 production backend）
  - **過程中發現並修正一個架構性問題**：`dev` 分支落後 `main` 19 個 commit（因為規則 11 原本的做法是前後端調整完直接 push `main`，沒人維護 `dev`），staging 剛部署時其實跑的是舊版（`<title>` 還是 Vite 預設骨架）。跟使用者確認後，把工作流程改成「先 push `dev` → 部署驗證 staging → 沒問題再 merge 進 `main` → 部署 production」，並用 `git push origin origin/main:dev` 把 `dev` fast-forward 追上 `main`（`dev` 原本沒有任何獨有 commit，乾淨快轉不會漏東西）
  - 同步更新 `CLAUDE.md` 規則 11，把 commit/push/部署流程從「push main→部署 production」改成「push dev→部署 staging→驗證→merge main→部署 production」六步驟，並記錄 staging/production 架構供之後查閱
  - 已知限制：這個 repo 的 GitHub webhook 仍是壞的，push 到 dev／main 都不會自動觸發部署，仍需手動 `railway up --ci`（staging 加 `--environment staging`）
- [x] 設定環境變數管理（本機慣例已定：`backend/.env.example`、`frontend/.env.example` 列出所有需要的變數名稱，實際值一律放 `.env`，已加入 `.gitignore` 不進版控；正式站/測試站的變數值待 Railway 帳號設定後填入其後台，不寫進程式碼）
- [x] 設定基本 CI/CD — 已新增 `.github/workflows/ci.yml`：push/PR 到 `main`／`dev` 時自動對 `frontend/`、`backend/` 各自跑 `npm ci` + `npm run build`（本機已驗證兩邊 build 皆成功）。**待辦：自動部署到 staging/production** 需要 Railway 帳號的 deploy token，之後在此 workflow 加一個 deploy job

**1.4 監控與基本維運工具**
- [x] 設定錯誤追蹤工具（Sentry）— 前端已裝 `@sentry/vue`，見 `frontend/src/main.ts`：
  - 只勾選 **Error monitoring**，刻意不啟用 Session Replay（表單會收集出生年月日/收入/財產/身心障礙與罕病狀態等敏感資料，開螢幕錄製有外洩風險，且會跟 `LegalNotice.vue` 已承諾的隱私權政策矛盾）、Tracing、Logging、Application Metrics（現階段沒有真實流量，開了也沒資料可看，且會提前耗用免費額度）
  - 設定 `sendDefaultPii: false`，不把使用者資訊或 HTTP 內容送給 Sentry，只保留錯誤堆疊本身
  - 只在正式 build（`import.meta.env.PROD`）才初始化，本機 `npm run dev` 不會回報，避免開發雜訊灌爆額度
  - DSN 存在 `frontend/.env` 的 `VITE_SENTRY_DSN`（已 gitignore），`.env.example` 只列變數名稱
  - 已用 `npm run build` + `vite preview` + Playwright 驗證：正式 build 載入時 Sentry 有成功初始化（`window.__SENTRY__` 存在）、無 console error
  - 後端（Fastify）已接上，見 `backend/src/instrument.ts`（獨立的 Sentry 專案/DSN，與前端分開）＋ `backend/src/server.ts`：
    - `import './instrument.js'` 放在檔案最頂端（必須在所有其他 import 之前），並在路由註冊完成後呼叫 `Sentry.setupFastifyErrorHandler(app)`（Sentry 官方 Fastify 整合方式，安裝精靈清單沒有列 Fastify，選的是通用 Node.js/Vanilla 再手動接上）
    - 只在 `SENTRY_DSN` 有值且 `NODE_ENV=production` 時才初始化，本機 `npm run dev` 不會回報；`dataCollection: { userInfo: false, httpBodies: [] }` 比照前端做法，不送使用者資訊或 HTTP 內容給 Sentry
    - DSN 存在 `backend/.env` 的 `SENTRY_DSN`（已 gitignore），`.env.example` 只列變數名稱；Railway 部署時需在後台環境變數設定 `SENTRY_DSN` 與 `NODE_ENV=production`
    - 已用獨立測試腳本（設 `NODE_ENV=production` 後 `Sentry.captureException()` + `Sentry.flush()`）驗證確實能把錯誤送到 Sentry 後端專案；`npm run build`／`npm test`（22 個測試）／`npm run dev` 手動啟動＋打 `/health`、`/api/form-options` 皆確認正常，測試檔案已清除
- [ ] 設定基本流量/使用量監控，避免被異常流量灌爆卻不知道（**待辦：需要 Railway 帳號**，上線後用 Railway 內建 metrics 觀察）

### 階段 2：資料庫與資料內容
4. [x] 設計資料庫 schema — 見 `backend/src/db/schema.ts`（Drizzle，已用 `drizzle-kit push` 實際套用到 Neon，確認 4 張表都已建立）：
   - `benefits`：福利項目主表，含 `eligibilityConditions`（JSONB，供階段 3 規則引擎讀取）、`sourceUrl`/`sourceExcerpt`/`lastVerifiedDate`、`isActive`（軟刪除，對應「疑似已下架不自動刪除」）
   - `benefitDocuments`：所需文件（一對多）
   - `benefitLocations`：申請地點（一對多）
   - `benefitChangeLog`：對應第四節「6. 版本紀錄」，`approvedAt` 為 null 代表待人工審核
   - Neon project：`welfare-check`（region: ap-southeast-1 新加坡），連線字串存在 `backend/.env`（已 gitignore，未進版控）
5. [ ] 把現有 SKILL.md 裡的 58 大類福利資訊轉換成結構化資料，人工初次建檔寫入資料庫
   - [x] **第 1 類（低收入戶/中低收入戶認定）已完成**，作為建檔流程的範例：
     - 新增 `backend/src/db/seed-data/`（seed 資料來源檔）與 `backend/src/db/seed.ts`（`npm run seed` 寫入腳本，可重複執行）
     - `benefits.county` 新增欄位，金額/門檻依縣市而定的類別可每縣市各存一筆
     - 已用 WebFetch 取得 115 年度（2026）官方公告 PDF（臺北市政府社會局），確認全國最低生活費其實只分 8 組數值（6 直轄市各一組＋臺灣省一組＋福建省一組，非每縣市都不同），22 縣市資料已全部寫入 Neon 並驗證正確
   - [x] **第 21、28、32、49、57 類已完成**（全國統一制度，不分縣市，各 1 筆）：勞保給付權益、勞工退休金自願提繳、配偶健保/國民年金加保確認、國民年金身心障礙年金、金管會金融優惠與消費者保護服務
   - [x] **第 43、44、46、50、15 類已完成**（全國統一制度，各 1 筆）：農業部老農/漁民補助、退輔會補助、教育部學費減免與就學貸款、就業保險臨時工作津貼、稅務減免。皆已用 WebSearch 查證官方來源後建檔；過程中發現 SKILL.md 第 15 類寫的「長照特別扣除額 12 萬」已過時（115 年度已調升至 18 萬），已同步修正 SKILL.md
   - [x] **第 41、42、45、51、56 類已完成**（全國統一制度，各 1 筆）：原民會補助、新住民發展基金補助、客委會補助、僑委會補助、現役軍人與軍眷福利。部分細項（如原民急難救助、客家子女才藝獎助學金）搜尋未查得明確金額，已如實標註 ⚠️ 待洽詢，未編造數字
   - [x] **第 19、48、23、36、25 類已完成**（全國統一制度，各 1 筆）：健保預防保健服務、兒童公費疫苗與預防保健、法律扶助、戒菸補助、通訊費優惠（NCC）
   - [x] **第 20、27、30、40、58 類已完成**（全國統一制度，各 1 筆）：水電費減免、外籍家庭看護工申請、文化幣、太陽能/綠能裝設補助、罕見疾病醫療與生活補助
   - [x] **第 9、14、34、47、55 類已完成**（全國統一制度）：長期照顧（長照3.0，1 筆）、貸款類補助（4 筆子項：新青安3.0/青年創業貸款/微型創業鳳凰貸款/勞工紓困貸款）、中高齡就業促進補助（1 筆）、發展遲緩兒童早期療育補助（1 筆）、天然災害救助（1 筆）
   - **目前累計：31 大類、55 筆資料已建檔**
   - [x] **第 7、10、8、6 類已完成**（重要發現：這幾類原本以為需依 22 縣市分別建檔，但實際查證後確認是**中央法規/方案統一訂定基準金額**，只是地方政府執行時可能加碼，不是每縣市完全獨立的數字）：
     - 育兒與托育補助（公托 7,000／準公托 13,000 全國統一，已取消排富）
     - 租金補貼（300 億元中央擴大方案，2,000～8,000 元依身分級距，可疊加至 14,400 元）——社會住宅當時因屬即時性招租資訊，一度整項排除在資料庫外，後續已依第 198 項修正為「即時內容不存，但穩定的申請入口/資格門檻要存」，詳見下方修正記錄
     - 老人福利／中低收入老人生活津貼（中央基準 8,329／4,164 元，已標註地方可能加碼）
     - 身心障礙補助／生活補助費（中央基本金額 4,700／3,500 元，已用新北市案例驗證地方確實常見加碼到更高金額，已誠實標註 ⚠️ 精確金額需依縣市查證）
   - **目前累計：35 大類、59 筆資料已建檔**
   - [x] **回頭補齊第 6、7、10 類的 22 縣市地方加碼**（使用者要求）：
     - 育兒與托育補助（7）：確認台北、新北、桃園、台中、新竹市有具體加碼金額，台南、高雄有加碼但金額未查得，其餘 15 縣市未查得地方加碼資訊，皆已誠實標註 ⚠️。共新增 22 筆縣市列（1 中央基準 + 22 縣市加碼＝23 筆）
     - 身心障礙補助（6）：中央基準金額更新為更完整的 4 級數字（極重度 8,836／重度 8,499／中度 5,065／輕度 4,872），確認新北市有明確加碼數字，其餘 21 縣市未查得，皆已標註 ⚠️（1 + 22 = 23 筆）
     - 租金補貼（10）：確認台北市「幸福租」、新北市「青年租金補貼」為具體加碼/自辦方案，其餘 20 縣市未查得，皆已標註 ⚠️（1 + 22 = 23 筆）
   - [x] **第 8 類（老人生活津貼）確認不需要 22 縣市分別建列**：查證後發現該津貼「發放金額」8,329／4,164 元全國統一，縣市間真正不同的只是「所得門檻」，而該門檻已存在第 1 類的 `referenceData.minLivingExpense`，比對時直接引用即可，已在 notes 說明此邏輯，避免重複建檔
   - **目前累計：35 大類、125 筆資料已建檔**（新增的 66 筆皆為第 6/7/10 類的縣市加碼資料列）
   - [x] **第 2、3、4、5、11 類已完成**：
     - 急難救助（4）、就業補助/產業人才投資方案（11）、特殊境遇婦女/家庭扶助（3）：確認為中央統一框架，各建 1 筆
     - 醫療與健保補助/健保費補助（5）：中低收入戶部分（50%/100%）全國統一，但「65 歲以上一般長者」健保費全額代繳確實因縣市而異，已確認南投縣/臺東縣/新竹市有此加碼，建立 1 中央基準 + 22 縣市列（23 筆）
     - 生育與孕產補助（2）：中央「生育給付 PLUS」每胎 10 萬元全國統一，但生育獎勵金確實因縣市（甚至鄉鎮）而異，已確認台北市/桃園市/苗栗縣（含通霄鎮案例）具體金額，建立 1 中央基準 + 22 縣市列（23 筆）
   - **目前累計：40 大類、174 筆資料已建檔**
   - [x] **第 16、17、31、35、39 類已完成**（全國統一制度，各建 1 筆）：配偶無收入相關補助、照顧者支持（家庭照顧假/喘息服務）、喪葬補助（勞保/國民年金）、人工生殖費用補助（不孕症試管嬰兒補助 3.0）、回流教育/成人進修補助。過程中發現 SKILL.md 第 35 類寫的「中低收入戶最高 $20 萬」已過時（3.0 新制已改為與一般戶同為 $15 萬，差別在於不限年齡與胎次），已同步修正 SKILL.md
   - **目前累計：45 大類、179 筆資料已建檔**
   - [x] **第 37、52、53、38、22 類已完成**：
     - 公共運輸通勤補助（37，TPASS 定期票）、住宅地震基本保險保費補助（38）：全國統一制度，各 1 筆
     - 數位發展部補助（52）：查證後發現 SKILL.md 原描述的「數位學習券」實為**教育部**終身學習券，非數位發展部業務，已在 notes 誠實標註張冠李戴問題，建議後續調整分類歸屬
     - 體育署補助（53）：改用個人可直接申請的「運動幣」（500 元，抽籤制）作為主要項目，運動 i 臺灣 2.0 主要補助對象為地方政府/團體而非個人
     - 環保節能補助（22）：電動機車購車補助中央基準全國統一，但地方加碼確實因縣市而異，已確認台北市/桃園市/新北市具體加碼金額，建立 1 中央基準 + 22 縣市列（23 筆）
   - **目前累計：50 大類、206 筆資料已建檔**
   - [x] **最後 6 大類已完成**：
     - 心理諮商補助（24）：查證後發現實為衛福部全國統一方案（15-45 歲青壯世代心理健康支持方案，3 次免費諮商，無戶籍限制），非縣市各自舉辦
     - 居家修繕與無障礙環境改善（26）：查證後發現實為 2026 年 3 月新核定的中央「老宅延壽計畫」（3 年 50 億元，全國適用），非縣市各自舉辦
     - 離島居民交通與醫療補助（54）：本質上只適用澎湖縣/金門縣/連江縣/臺東縣（蘭嶼鄉、綠島鄉）4 個縣市，已依「離島地區居民航空票價補貼辦法」建立 4 筆縣市明細
     - 老人其他福利（18，重陽禮金/敬老卡）：確認因縣市而異，已查證新竹縣（禮金 1 萬全台最高）、桃園、台中、雙北、新竹市、台東縣（敬老卡點數）具體數字，建立 1 全國概況 + 22 縣市明細（23 筆）
     - 食物銀行（29）：確認由地方政府各自訂定資格，已查證台中市範例，其餘縣市誠實標註未查得，建立 1 全國概況 + 台中確認列 + 21 縣市待查列（23 筆）
     - 縣市政府急難慰問金（33）：確認因縣市而異，但本次搜尋只查到「端午節/年節慰問金」（性質不同，已在 notes 說明兩者差異並建議未來另行查證補入 SKILL.md），未查得急難慰問金逐縣市數字，誠實建立 1 全國概況 + 22 縣市待查列（23 筆），未編造金額
   - **✅ 階段 2 第 5 項全部完成：58 大類中，56 大類已建檔（扣除第 12「地方政府專屬補助」與第 13「非同戶籍家人適用補助」兩個本質上非獨立福利項目），累計 281 筆 `benefits` 資料寫入 Neon，已驗證 `distinct category_number = 56`**
   - [x] **修正社會住宅的處理原則**（使用者提出質疑後修正）：原本判斷「社會住宅空戶即時變動，不適合存資料庫」沒有錯，但錯誤地把整個社會住宅項目都排除在資料庫外——這樣使用者在網站上會完全查不到這項福利的存在。修正原則：**「即時性內容不存，但穩定的『去哪裡查/基本資格』要存」**。已在第 10 類新增社會住宅索引資料：全國「安居好室」平台（含資格門檻）1 筆＋22 縣市申請入口列（已確認台北/新北/桃園/台中/台南/高雄有獨立入口網站，其餘縣市導引至全國平台），每筆皆誠實標註「目前空戶與否需至入口網站即時查詢」。累計新增 23 筆
   - [x] **回頭全面稽核是否有其他類別跟社會住宅一樣被完全遺漏**（使用者要求）：Grep 所有 56 大類的 seed 資料檔，找出「⚠️ 金額未查得」（可接受，已誠實標註）跟「整個子項目 0 筆」（不可接受，等同對使用者隱形）兩種情況的差異。發現**第 2 類（生育與孕產補助）** 有相同等級的漏洞：SKILL.md 明確列出的「孕產婦補助/坐月子」與「流產假/流產心理諮商補助」兩個子項，先前只建了「生育獎勵金」的縣市資料，這兩項完全是 0 筆。已補上：
     - 流產假（全國統一，依性別工作平等法，1 筆）
     - 孕產婦補助/坐月子補助（22 縣市地方方案列，皆誠實標註金額未查得，並註明與生育獎勵金是不同的獨立方案）
   - [x] **更嚴謹逐條核對 SKILL.md 原始子搜尋清單後，再找出 6 個同等級漏洞並補上**：
     - 第 5 類：健保重大傷病卡免部分負擔（全國統一，1 筆）
     - 第 18 類：中低收入老人假牙補助（衛福部中央補助已退場，改地方自訂，1 全國概況 + 22 縣市明細）、身心障礙者愛心卡乘車優惠（1 全國概況 + 22 縣市明細，已確認台東/台中/雙北點數）
     - 第 22 類：節能家電汰換補助（冷氣/冰箱/除濕機每台 5,000 元，全國統一，1 筆）、電動自行車購車補助（1 中央基準 + 22 縣市加碼列，已確認新北市具體金額）
     - 第 54 類：離島就醫機票/醫療代金補助、離島就學生交通補助（各 4 個離島縣市皆建列，誠實標註金額未查得）
     - 第 56 類：國軍隨眷就業（1 筆，誠實標註申請細節未查得）
     - 過程中也修正一處建檔疏漏：電動自行車的 22 縣市加碼列漏了桃園市（陣列拼接錯誤），已修正為完整 22 縣市
   - [x] **第三輪逐條核對（用新寫的機械化清單方法）**：把 SKILL.md 每一類的每一行「搜尋：」語句都抄出來，逐條比對 seed 資料，又抓到 2 個完全零筆的漏洞：
     - 第 21 類：職業災害勞工保護補償（未加保勞工遭遇職災、雇主未依勞基法補償時的申請管道，全國統一，1 筆）
     - 第 30 類：文化部推動實體書店發展補助——查證後發現這其實是**書店經營者**的補助（非個人閱讀補助），已誠實標註此澄清，避免使用者誤以為有個人適用的閱讀補助
     - 第 35 類：補充說明人工生殖補助僅涵蓋試管嬰兒（IVF），體外授精（IUI）不在補助範圍內，避免使用者誤解
   - [x] **把第 11、16、17、20、25、27 類「已標註⚠️但未建成縣市列」的子項目也升級成完整縣市列**（使用者要求）：
     - 第 16 類：查證後發現「二度就業婦女」其實是勞動部**全國統一**的「婦女再就業計畫」（自主訓練獎勵/再就業獎勵每滿30日1萬最高6萬/職場支持輔導獎勵每月5,000元），改建為全國統一 1 筆，不需要 22 縣市列
     - 第 27 類：查證後確認「中低收入戶外籍看護薪資補助」也是**全國統一**制度（就業安定費季度自動比對減免、經濟弱勢雇主薪資補助），新增全國 1 筆；仲介費補助部分仍未查得縣市數字，建立 22 縣市待查列
     - 第 17 類（照顧者現金津貼）、第 20 類（瓦斯費補助）、第 11 類（工作地點縣市勞工局補助）：本次搜尋仍未查得具體縣市數字，依使用者要求建立 22 縣市待查列，皆誠實標註 ⚠️
     - 第 25 類：深入查證後發現 SKILL.md 列的「中低收入戶電話費補助」跟「NCC電信優惠」搜尋語句實際上指向同一個電信業者優惠方案，並非兩個不同的福利項目，因此**沒有**建 22 縣市列，改為在既有資料加註澄清（若之後真查到獨立的縣市電話費現金補助，需另外建檔）
     - 新增共用檔案 `backend/src/db/seed-data/counties.ts`，把 22 縣市清單抽成共用常數，避免各檔案各自重複列舉時再度漏掉某個縣市（先前電動自行車就漏過桃園市）
   - **目前累計：56 大類、499 筆資料已建檔**
6. [x] 建立資料版本/異動紀錄機制（log 表）— 已包含在 schema 裡的 `benefitChangeLog` 表

### 階段 3：資格比對邏輯（取代 LLM 推理）
7. [x] 把 SKILL.md 裡每類福利的資格判斷規則，轉寫成結構化邏輯 — 見 `backend/src/logic/eligibility.ts`：`EligibilityConditions` 定義通用條件形狀（年齡範圍、所得門檻、人均動產門檻、身分別、地區、有無自有住宅、未成年子女/長者年齡、其他布林旗標），`evaluateEligibility()` 依 SKILL.md 第四步邏輯回傳 confirmed/possible/not_eligible（❌ 優先於 ⚠️）。已隨階段 2 第 5 項的 56 大類建檔同步填入 `benefits.eligibilityConditions`。
    - **重要修正（使用者發現的設計缺陷）**：原本 `requiredFlags` 沒勾就直接判定 `not_eligible`，整筆從結果消失。但使用者指出：對話式 SKILL.md 是逐題問（使用者被動回答），網站表單卻要使用者自己在一堆勾選項裡認出「這個補助跟我有關」才會勾——使用者根本不知道有這個補助存在時就不會勾，等於永遠看不到。修正為：旗標沒勾一律視為「不確定」而非「確定沒有」，改列進 `⚠️ 可能符合`（並列出缺少哪個具體旗標的中文說明，而非籠統的「其他條件旗標」），不會再整筆濾掉不顯示。身分別（`requiredIdentities`，如原住民/退伍軍人）維持原本邏輯不變，因為那類身分使用者通常很清楚自己是不是，跟需要額外覺察的細節狀況（戒菸意願、人工生殖需求等）性質不同。已補 3 個新測試案例（共 25 個），實測比對：同一組假資料，`possible` 從 0 筆變成 45 筆，這些原本會被永久隱藏的項目現在都能被使用者看到並自行確認
    - **第二個真實缺陷（使用者實測發現）**：勾選「無自有住宅」後，仍然看到「居家修繕與無障礙環境改善（老宅延壽計畫）」被列為確定符合。追查發現：`EligibilityConditions` 型別只有 `requiresNoOwnedHome`（需無自有住宅），沒有對應的「需要自有住宅」條件；`backend/src/db/seed-data/26-home-renovation-subsidy.ts`、`40-solar-energy-subsidy.ts`（太陽能屋頂型光電）這兩筆的 `description` 都寫明「須為房屋所有權人」「需建物所有權證明」，卻完全沒有設定 `eligibilityConditions`，等於資料庫沒有任何限制，人人都會被列為確定符合。已新增 `requiresOwnedHome` 條件與 `checkOwnedHome()` 檢查函式，並補上這兩筆的 `eligibilityConditions: { requiresOwnedHome: true }`；用 `grep` 全庫搜尋「建物所有權/房屋所有/需為屋主/屋主出面/不動產所有」確認只有這兩筆受影響，沒有遺漏其他類別。已補 2 個測試案例（共 27 個），並重新執行 `npm run seed` 把修正寫進 Neon 正式資料庫（因為 `eligibilityConditions` 存在資料庫裡，改 TypeScript 原始檔不會自動生效），實測租屋者（`ownsHome:false`）不再看到這兩項、有自有住宅者（`ownsHome:true`）仍正常看到
8. [x] 設計「應計算人口」「人均所得」「動產總額」等共用計算邏輯 — 見 `backend/src/logic/calculations.ts`（`calculateAge`、`daysUntilAge`/`yearsUntilAge`、`calculatePerCapitaMonthlyIncome`、`calculatePerCapitaAssets`、`evaluateIncomeThreshold`、`meetsAssetThreshold`，對應 SKILL.md 第二步 A～E 公式）
9. [x] 撰寫單元測試 — `backend/src/logic/__tests__/`，共 25 個測試案例（calculations 13 個、eligibility 12 個），已加入 `.github/workflows/ci.yml` 的 `backend-test` job，`npm test` 全數通過。**待辦**：日後累積真實受查者案例（去識別化後）可再補充成回歸測試

### 階段 4：前端使用者流程

**（2026-07-29 重新設計，取代原本 MVP 單頁表單版本）** 使用者實測 MVP 版本後指出兩個核心問題：(1) 網站版用一次性大勾選清單取代原本逐題問答，使用者不知道有什麼補助存在就不會勾，等同永遠看不到；(2) 輸出行為沒有比照原本對話式流程的「文件去哪取得／申請地點地址網址／🔴🟠🟡 優先順序／補助總覽表」完整規格。確認範圍後（逐人登記家庭成員戶籍狀態、第 13 題非同戶籍家人跑完整資格比對），全面重做：

10. [x] 設計逐題引導式問卷 UI（比照原本 SKILL.md 18 題／含子題）— 見 `frontend/src/components/Questionnaire.vue`：
    - 一次一題、有進度顯示（第 N 題／共 N 題），條件跳題比照原規格（例如第 10a 題僅在受僱/自營時詢問，家庭成員相關題目在無成員時整組跳過）
    - 第 5/6 題改成逐人登記（關係、出生年月日、設籍縣市、是否同戶號），第 8/9 題財產與收入依第 6 題結果動態列出應計算人口，第 12 題健康狀況依第 5 題名單逐人詢問（含不同戶籍者）
    - 第 13 題非同戶籍家人做成可重複新增的子表單（關係/出生年月日/戶籍縣市/年收入/動產/是否單人戶）
    - 新增 `backend/src/logic/questionnaire.ts`：`deriveFlags()` 把逐題結構化答案直接轉換成 27 個 `requiredFlags`（例如第 10 題就業身分直接推導 `is_employed`/`unemployed`/`has_labor_insurance`，第 14a 題勾選直接對應創業/生育/戒菸/法律諮詢旗標），使用者不需要再自己從一堆勾選項裡認出要勾什麼；`hasChildUnder`/`hasElderOver` 也改成從第 5 題家庭成員出生日期自動算出，不再另外詢問
    - 原本 MVP 版本的 27 項「其他狀況與需求」大勾選清單、`FLAG_OPTIONS` 前端曝光已移除；`FLAG_OPTIONS` 只保留在後端供「⚠️ 可能符合」列表的中文標籤顯示用
    - 已刪除 `WelfareForm.vue`（單頁表單，完全被取代）
11. [x] 串接前端逐題答案 → 後端 API → 資料庫比對（含非同戶籍家人）→ 回傳結果 — 重寫 `backend/src/routes/check.ts`：
    - `POST /api/check` 改收完整 18 題答案，對本人與每位第 13 題非同戀籍家人**各自跑一次完整 `evaluateEligibility`**（家人資料有限，只用戶籍縣市/年齡/若為單人戶的所得動產比對，其餘條件誠實列入⚠️需自行確認，不虛構未收集的資料）
    - 新增優先順序計算：🔴 有時限（不論確定/可能符合皆優先）、🟠 確定符合、🟡 可能符合，比對結果依此排序
    - 補上文件的 `obtainLocation`（去哪裡取得）欄位吐出（schema 早就有這個欄位，先前 API 忘記回傳），前端有資料才顯示，沒有就不顯示，不編造內容
    - 新增 `backend/src/logic/__tests__/questionnaire.test.ts`（13 個測試案例，涵蓋旗標推導與年齡自動計算），加上既有測試共 40 個全數通過
    - 已用 curl 對真實資料庫驗證完整 18 題請求／多人比對／優先順序正確
12. [x] 重新設計結果頁面 — 見 `frontend/src/components/ResultsView.vue`＋新增 `BenefitCard.vue`：
    - 頂端新增「補助總覽表」：依對象（本人／各非同戶籍家人）分兩欄列出 ✅ 確定可申請／⚠️ 建議確認的補助名稱，比照原本規格
    - 每張補助卡片顯示 🔴/🟠/🟡 優先順序圖示，取代原本只有「有時限」單一標籤
    - 每位第 13 題家人各自獨立一個區塊，附上「資料有限」的說明提醒使用者親自確認
    - 已用 Playwright 完整實測（18 題逐題填寫→送出→結果頁），確認條件跳題、家庭成員/非同戶家人子表單、優先順序、總覽表皆正常運作，無 console error

### 階段 5：法務與信任文案
13. [x] 撰寫隱私權政策 — 見 `frontend/src/components/LegalNotice.vue`：說明填表資料即查即丟、後端不寫入資料庫、不建帳號/不使用追蹤 cookie，下載的 HTML 報告只存在使用者自己裝置
14. [x] 撰寫免責聲明 — 同見 `LegalNotice.vue`：說明比對結果僅供參考不具法律效力、✅/⚠️ 標籤的意義與限制、資料庫每週更新但個別項目可能仍有落差
15. [x] 網站頁面標示「資料更新日期」— 後端 `/api/check` 回傳 `generatedAt`（查詢當下日期）與 `oldestVerifiedDate`（本次結果中最舊的單筆查證日期，刻意取最舊而非最新，避免用一筆新資料掩蓋其他項目可能更久沒查證的事實），前端顯示於結果頁頂端與每張福利卡片各自的查證日期

### 階段 6：上線前測試
16. [ ] 找幾位真實使用者（不同身份情境）測試填表流程與結果準確性（需要人工找測試使用者，非本次可自行完成）
17. [x] 確認手機/桌面瀏覽器顯示正常、結果頁可正確下載 — 新增 `frontend/src/reportDownload.ts`（自包含 HTML 報告，`welfare-result-YYYYMMDD.html` 命名對應 SKILL.md 原本輸出規格）與 `window.print()` 列印按鈕（`@media print` 隱藏表單/按鈕，只印結果內容）。已用 Playwright 對 iPhone SE／iPhone 13／Pixel 5／iPad Mini 四種裝置尺寸實測填表→送出→結果頁全流程：
    - 過程中抓到並修正兩個手機版排版 bug：(1) 320px 窄螢幕下福利卡片裡的長網址連結會撐開版面造成橫向捲動，加上 `overflow-wrap: anywhere` 修正；(2) 家庭成員收入/動產輸入框在窄螢幕下因 `<input>` 原生內容寬度蓋過 flex 版面設定造成溢出，需明確設定 `flex-basis:0` 才會照 flex 容器邏輯縮放而非用瀏覽器預設寬度
    - 結果頁下載/列印按鈕在 ≤480px 寬度時改為直排滿版（原本並排靠右在窄螢幕看起來像漂浮的按鈕，體驗不佳）
    - 全程用 `document.documentElement.scrollWidth - clientWidth` 量測確認四種裝置皆無橫向溢出（0px），且無 console error

### 階段 7：上線後維運（銜接第四節）
18. [x] 建立定期人工審核更新流程 — 依使用者選擇「擴充現有排程」而非另建新排程：把既有 `welfare-check-threshold-update` 雲端排程（trigger id `trig_01JSQPCpLYLsShUoTGekN7KL`，每週日凌晨 1 點）的 prompt 從原本「只檢查 SKILL.md」擴充為第 4 項檢查「逐筆重新查證 `backend/src/db/seed-data/*.ts` 裡每筆 `SeedBenefit` 的 `sourceUrl`，異動就改檔案＋更新 `lastVerifiedDate`，來源查證失敗則加註 ⚠️ 不刪除」：
    - 沿用同一套「開分支 `update-thresholds-YYYYMMDD` ＋ 更新紀錄留檔 ＋ push 分支不動 main」流程，`更新紀錄/` 的 log 格式新增「資料庫 seed 資料異動」「疑似下架/來源失效（seed-data）」兩個章節
    - **刻意設計成排程只改 seed-data 的 TypeScript 原始檔，不執行 `npm run seed`、不連線 Neon**——完全對應第四節「AI 的角色是找差異+附證據，寫入資料庫的決定權永遠在人工」的原則；人工 review 該分支後，自己執行 `cd backend && npm run seed` 才會真正把異動寫進正式資料庫
    - 沒有另外新建排程（節省重複的雲端執行成本），也沒有更動原本的觸發頻率

### 後續（暫緩，本次規劃不展開）
19. [ ] 規劃 Claude API 自動化批次更新腳本（取代手動跑 Claude Code）
20. [ ] 評估並串接金流（含商業登記等前提）
